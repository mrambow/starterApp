import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sinc.cases.ebeihilfe',
  appName: 'starterApp',
  webDir: 'www/browser',
  server: {
    androidScheme: 'https'
  }
};

export default config;
